<p style="color: green"><%= notice %></p>

<% content_for :title, "News" %>

<h1>Últimas Noticias</h1>

<div id="news">
  <% @news.each do |news| %>
    <h2><%= link_to news.title, news_path(news) %></h2>
    <p><%= truncate(news.content, length: 100) %></p>
    <p><%= link_to "Mostrar esta noticia", news %></p>
  <% end %>
</div>

<p><%= link_to "Nueva noticia", new_news_path %></p>
